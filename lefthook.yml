pre-commit:
  commands:
    format:
      run: npm run format
    tsc:
      run: npm run tsc
    test:
      run: npm test -- --run
pre-push:
  commands:
    build:
      run: npm run build
    test:e2e:
      run: npm run test:e2e
